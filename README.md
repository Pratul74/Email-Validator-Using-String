# Email Validator using Tkinter 📨

This is a simple Python GUI application that validates an email address using basic logic rules. It is built using the `tkinter` library.

## 🔧 Features

- Validates the structure of an email address.
- Basic checks like:
  - Email length (>=6 characters)
  - Must start with an alphabet
  - Contains exactly one '@'
  - Has a '.' in correct position
  - No invalid characters or spaces
- Simple user interface with input field and result display.

## 🖥️ GUI Screenshot

*(Insert screenshot here if you'd like)*

## 🛠️ Requirements

- Python 3.x
- tkinter (comes pre-installed with Python)

## 📦 How to Run

1. Clone the repository:

```bash
git clone https://github.com/your-username/email-validator.git
cd email-validator
```

2.Run the script:
```bash
python email_validator.py
```
